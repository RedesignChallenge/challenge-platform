<nav id="top-nav" class="navbar navbar-center" role="navigation">
  <div class='container'>

    <div class="navbar-header">
      <button id='navbar-hamburger' type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-nav">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to I18n.t(:project_name), root_path, class: 'navbar-brand' %>
      
      <!-- Custom link for new designs with challenge links in navigation -->
      <div class='navbar-center-link'>
        <%= link_to(@challenge.title.ellipsisize(15,15), challenge_path(@challenge)) if @challenge %>
      </div>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id='collapse-nav'>
      
      <ul class="nav navbar-nav">
        <li><%= link_to "About Us", about_path %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li id="user" class="dropdown pull-right">
            <a href="#" class="dropdown-toggle user" data-toggle="dropdown">
              <span style='padding:0px 15px;position:relative;top:1px'><%= current_user.first_name %></span>
              <% if current_user.avatar.present? %>
                <%= image_tag(current_user.avatar.thumb.url, width: 30, class: 'img-circle') %>
              <% else %>
                <span class="fa-stack fa-lg new-user-avatar">
                  <i class="fa fa-circle fa-stack-2x"></i>
                  <i class="fa fa-inverse fa-stack-1x">you</i>
                </span>
              <% end %>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "<i class='fa fa-gears'></i> Settings".html_safe, edit_user_registration_path %></li>
              <li class='divider'></li>
              <li><%= link_to "<i class='fa fa-sign-out'></i> Sign Out".html_safe, destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign In", new_user_session_path, class: 'text-bold' %></li>
          <li><%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-sm btn-info btn-rounded' %></li>
        <% end %>
      </ul>

    </div>
    <!-- /.navbar-collapse -->

  </div>
</nav>