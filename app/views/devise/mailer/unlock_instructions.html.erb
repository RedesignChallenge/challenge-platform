<%= render partial: 'mailers/shared/text', locals: { content: "Your account has been locked due to an excessive number of unsuccessful sign in attempts. Click the button below to unlock your account." } %>

<%= render partial: 'mailers/shared/button', locals: { url: unlock_url(@resource, unlock_token: @token), action: 'Unlock My Account' } %>