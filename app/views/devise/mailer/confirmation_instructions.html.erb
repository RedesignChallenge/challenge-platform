<%= render partial: 'mailers/shared/text', locals: { content: "You can confirm your account email through the button below:" } %>

<%= render partial: 'mailers/shared/button', locals: { url: confirmation_url(@resource, confirmation_token: @token), action: 'Confirm My Account' } %>